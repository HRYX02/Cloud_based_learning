server:
  port: 8081
spring:
  application:
    name: service-oss
  profiles:
    active: dev

# 阿里云 OSS
# 不同的服务器，地址不同
aliyun:
  oss:
    file:
      endpoint: oss-cn-beijing.aliyuncs.com
      keyid:
      keysecret:
      #bucket可以在控制台创建，也可以使用java代码创建
      bucketname: sxxstarbucket